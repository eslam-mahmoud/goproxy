version: "3.3"
services:
  goserver:
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - 8080:8081
    environment:
      LISTEN: ":8081"
      PROVIDERS: "api.giphy.com:443,google.com:443"